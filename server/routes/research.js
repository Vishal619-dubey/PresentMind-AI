import {Router} from "express";import {structuredAI} from "../ai.js";const router=Router();
router.post("/generate",async(req,res)=>{const {field="Artificial Intelligence",goal="Academic research"}=req.body;const fallback={title:`AI-Driven ${field} Framework for Practical Decision Support`,researchGap:`Existing ${field} solutions often lack explainability, accessible datasets and real-world validation.`,objectives:[`Design a practical ${field} framework`,`Evaluate accuracy and usability`,`Compare with existing approaches`],methodology:"Literature review, dataset collection, preprocessing, model or system development, experimental evaluation and user validation.",expectedOutcome:"A validated prototype, measurable performance results and recommendations for future research.",tools:["Python","Jupyter Notebook","MongoDB","React","Statistical evaluation"],datasets:["Public benchmark dataset","Institutional sample dataset","Synthetic validation dataset"],keywords:[field,"Artificial Intelligence","Decision Support","Evaluation"]};const prompt=`Return JSON with title,researchGap,objectives array,methodology,expectedOutcome,tools array,datasets array,keywords array for field "${field}" and goal "${goal}".`;res.json(await structuredAI(prompt,fallback))});export default router;
